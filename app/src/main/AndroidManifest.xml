<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true" />
    <!-- Камера — только для сканера QR-приглашений; без неё приглашение можно вставить ссылкой. -->
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />

    <!-- Android ≤ 11: install-time разрешения Bluetooth и вынужденная геолокация —
         без неё старая система не отдаёт результаты BLE-сканирования.
         CoarseFineLocation касается Android 12+, где геолокация не запрашивается вообще. -->
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30"
        tools:ignore="CoarseFineLocation" />

    <!-- Android 12+: neverForLocation — приложение не выводит местоположение из
         результатов сканирования, поэтому геолокация не запрашивается вообще. -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Только для проверки обновлений по кнопке (GitHub Releases) и установки
         скачанного APK. Другого сетевого кода в приложении нет. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Сканер QR-приглашений; запрашивается в рантайме только на экране сканера. -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".StreetPassApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.StreetPass">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Собственная схема для приглашений: открывается только приложением, без
                 выбора браузера. Именно её несут QR-код и кнопка «Поделиться». -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="streetpass"
                    android:host="friend" />
            </intent-filter>
            <!-- Ссылка-приглашение ведёт на страницу проекта: без приложения человек попадает
                 на GitHub и может его скачать, с приложением — система предложит открыть здесь.
                 Домен не наш, поэтому верифицированного App Link нет; на Android 12+ ссылка
                 откроется в браузере, пока пользователь не разрешит её в настройках приложения —
                 поэтому есть ещё приём через «Поделиться» и ручная вставка. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="github.com"
                    android:pathPrefix="/SworderZ/streetpass" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <service
            android:name=".ble.DiscoveryService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <receiver
            android:name=".ble.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
